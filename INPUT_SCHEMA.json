{
  "title": "India Telecom Plans Scraper Input",
  "description": "Collect public prepaid plan data from the official Jio, Airtel, and Vi websites.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "operators": {
      "title": "Operators",
      "description": "Select one or more Indian telecom operators.",
      "type": "array",
      "editor": "select",
      "default": ["jio", "airtel", "vi"],
      "items": {
        "type": "string",
        "enum": ["jio", "airtel", "vi"],
        "enumTitles": ["Jio", "Airtel", "Vi"]
      }
    },
    "categories": {
      "title": "Plan filters",
      "description": "Optional text filters such as 5G, annual, OTT, data, or popular. Matching is case-insensitive.",
      "type": "array",
      "editor": "stringList",
      "default": []
    },
    "minPrice": {
      "title": "Minimum price (INR)",
      "description": "Only save plans at or above this price.",
      "type": "number",
      "editor": "number",
      "minimum": 0,
      "default": 0
    },
    "maxPrice": {
      "title": "Maximum price (INR)",
      "description": "Only save plans at or below this price.",
      "type": "number",
      "editor": "number",
      "minimum": 0,
      "default": 100000
    },
    "maxResults": {
      "title": "Maximum plans",
      "description": "Maximum unique plans saved across all selected operators.",
      "type": "integer",
      "editor": "number",
      "minimum": 1,
      "maximum": 500,
      "default": 100
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "description": "India residential proxies are recommended for reliable regional access to Airtel and Vi.",
      "type": "object",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"],
        "apifyProxyCountry": "IN"
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"],
        "apifyProxyCountry": "IN"
      }
    }
  }
}
